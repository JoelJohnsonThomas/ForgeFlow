apiVersion: v1
kind: Service
metadata:
  name: forgeflow-mcp
  namespace: forgeflow
  labels:
    app.kubernetes.io/name: forgeflow
    app.kubernetes.io/component: mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: forgeflow
    app.kubernetes.io/component: mcp
  ports:
    - name: http
      port: 8001
      targetPort: http
