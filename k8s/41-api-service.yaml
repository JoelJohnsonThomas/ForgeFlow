apiVersion: v1
kind: Service
metadata:
  name: forgeflow-api
  namespace: forgeflow
  labels:
    app.kubernetes.io/name: forgeflow
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: forgeflow
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8000
      targetPort: http
